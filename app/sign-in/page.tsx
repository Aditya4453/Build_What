"use client";
import { useRouter } from "next/navigation";
import { Shell } from "@/components/shell";
import { useFlow } from "@/components/flow-provider";
export default function Page(){const r=useRouter(),f=useFlow();const enter=(create:boolean)=>{f.signIn({name:create?"Aarav Sharma":"Aarav Sharma",email:"aarav.sharma@example.test"});r.push("/applications")};return <Shell><section aria-labelledby="sign-in-title" className="mx-auto flex min-h-[calc(100vh-120px)] max-w-lg flex-col justify-center px-4"><p className="text-xs font-bold uppercase tracking-widest text-secondary">Demo citizen account</p><h1 id="sign-in-title" className="mt-2 text-3xl font-bold text-primary">Continue to your applications</h1><p className="mt-3 text-muted">Use synthetic data only. This does not create a real government account.</p><div className="card mt-7 p-6"><p className="font-bold">Aarav Sharma</p><p className="text-sm text-muted">aarav.sharma@example.test</p><button onClick={()=>enter(false)} className="btn-primary mt-6 w-full">Sign in with demo account</button><button onClick={()=>enter(true)} className="btn-secondary mt-3 w-full">Create demo account</button></div></section></Shell>}
